import React from 'react';
import IconMaterialIcons from "react-native-vector-icons/MaterialIcons";

export default SBIconFont = (props) => {
    return (
        <IconMaterialIcons {...props} />
    )
}